/*
	Name:		  6.16.c
	Author:       Yunus Korkmaz
	Date:	   	  20.07.20 00:07
	Description:  Label the elements of 3-by-5 double-subscripted array sales to indicate the order in which
					they’re set to zero by the following program segment:
							for ( row = 0; row <= 2; ++row ) {
							 for ( column = 0; column <= 4; ++column ) {
							 sales[ row ][ column ] = 0;
							 }
							}
*/

	-first loop control row and second loop control columns 
	-every time second loop do same work, first loop work and than second loop fiil the columns which belong that row 

